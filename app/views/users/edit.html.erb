<div class="Edit">
	<h2>Edit Profile </h2>
	 <div class="row">
   		<div class="col-6">
	<%= form_for(@user) do |form| %>
		<%= render partial: '/users/form', object: form %>

  	<% end %>


  	 	</div>
	
		<div class="col-6">
			<div class= "profile-pic-container">
			<% if @user.avatar.url != nil%>
			 <%= image_tag @user.avatar.url, id: "profilepic"%> 
			
			<% end %>

			</div>
		</div>

	</div>

</div>
